-- | This module exposes the concrete instances to run a blockchain against a
-- real node backend, mirroring 'Cooked.MockChain.Instances' which targets the
-- emulated chain.
module Cooked.BlockChain.Instances () where
